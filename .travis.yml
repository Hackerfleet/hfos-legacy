sudo: required
language: python
service:
  - docker
before_install:
  - echo "Would get the container now. Happily not doing that."
before_script:
  - sleep 15
script:
  - echo "I am the test procedure :)"
after_success:
  - coveralls
notifications:
  irc:
    channels:
      - "chat.freenode.org#hackerfleet"
    on_success: change
    use_notice: true
    skip_join: true
    template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message} ( %{duration}, %{build_url} )"
  email:
    recipients:
        - riot@c-base.org
    on_success: change

